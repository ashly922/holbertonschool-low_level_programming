#include <stdio.h>
/**
 * main - prints name of program
 * @argv: argument vector
 * @argc: number of arguments
 * Return: (0)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
