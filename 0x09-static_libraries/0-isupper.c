#include <stdio.h>
#include "main.h"

/**
	* _isupper - checks for uppercase
	* main - check the code
	* @c: lowercase
	* Return: (1) for success (0) for fail
	*/
int _isupper(int c)

{

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
}
}
